# Движок «{{TITLE}}» (`{{SLUG}}`)

> Паспорт движка: что собираем, в каких границах, чем закончим и где сейчас находимся. Машиночитаемая копия состояния — `engine.json`; при расхождении верна она, а этот файл правится следом.

| Параметр | Значение |
|---|---|
| Корень | `{{ROOT}}` |
| Дата сборки | {{TODAY}} |
| Язык корпуса | `{{LANGUAGE}}` |
| Профиль | `{{PROFILE}}` |
| Финиш | `{{FINISH}}` |

## Миссия

{{MISSION}}

Этот текст вставляется дословно в каждый промпт конвейера. Меняешь миссию — меняй её в `engine.json` и перерендери задачи (`render-tasks.mjs --force`), иначе агенты будут работать по старой.

## Периметр

**Внутри:** {{PERIMETER_IN}}

**Вне:** {{PERIMETER_OUT}}

Материал из-за периметра берётся только как переносимый слой — механика, которая работает и здесь, — и помечается в отчётах явно. Периметр подтверждается человеком до первого прогона: расширять его после того, как написаны ТЗ, дороже, чем собрать движок заново.

## Закон конвейера

Ни один шаг не сжимает содержание. Отчёт полнее ТЗ, свод — надмножество относящихся к нему отчётов, методика — навигатор поверх сводов. Ступени дайджестов нет. Единственное допустимое сокращение — склейка дословных повторов между источниками с пометкой «подтверждено N отчётами».

## Темы

Статусы: `planned` → `researched` → `verified`.

| # | Тема | Что внутри | Кормит своды | Статус |
|---|---|---|---|---|
| `<NN-slug>` | `<название темы>` | `<одна строка: что внутри>` | `<label свода>` | `planned` |

Тема, не питающая ни одного свода, — ошибка декомпозиции: либо она лишняя, либо не хватает свода.

## Своды

Статусы: `planned` → `done`.

| Label | Файл | Назначение | Primary | Secondary | Статус |
|---|---|---|---|---|---|
| `<label>` | `synthesis/<FILE>.md` | `<что закрывает>` | `<NN-slug>` | `<NN-slug>` | `planned` |

Свод без primary-темы — ошибка: его нечем наполнить.

## Журнал решений

Сюда пишутся решения, которые не видны из файлов: почему тема разрезана так, почему периметр сужен, что решили не исследовать и по какой причине, где сознательно пошли на пробел. Одна строка — одно решение, с датой.

| Дата | Решение | Причина |
|---|---|---|
| {{TODAY}} | Движок заведён: профиль `{{PROFILE}}`, финиш `{{FINISH}}` | `<чем обоснован выбор>` |

## Прогоны

Полный журнал — `state/run-log.md` (что, когда, каким хостом, сколько). Здесь достаточно последней строки по каждой стадии.

| Стадия | Когда | Хост | Итог |
|---|---|---|---|
| `<stage>` | `<дата>` | `<claude / codex>` | `<сколько задач, сколько упало>` |

## Следующая команда

`<команда>` — `<что она сделает>`

Порядок стадий: `plan` (декомпозиция на ТЗ) → `run research` → `run verify` → `run synth` → `run merge` (если банк собирается частями) → `run critique` → при финише `method` ещё `run method` и `run agent-doc`. Между стадиями — `audit` по корню движка: он проверяет закон несжатия и ловит пропущенные отчёты и битые ссылки до того, как на них построят следующий этаж. Прогоны — партиями по 3–4 задачи: девять тяжёлых агентов разом выжигают лимит сессии.
